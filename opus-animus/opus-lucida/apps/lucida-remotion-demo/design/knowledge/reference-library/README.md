# Approved reference evidence

Each directory is a canonical, reviewable reference package. Only a package with an approved source record and approved document observations is compiled. Raw collector output remains under `pipeline/runs/` and must be sanitized and explicitly approved through `knowledge:promote` before it can enter this library.

The committed fixtures are local, rights-cleared test evidence. They deliberately point to stable project artifacts and do not vendor third-party media.
