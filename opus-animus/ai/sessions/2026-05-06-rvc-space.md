# Session Log — 2026-05-06 — RVC Space

## Mục tiêu
Fix HuggingFace Space `chihuy/lucida` để convert 17 slide audio qua Lucida RVC model.

## Đã làm

### Đã giải quyết
- Xóa Gradio hoàn toàn → FastAPI thuần (fix `bool not iterable` schema bug)
- Fix `load_model()` → dùng `rvc_models/lucida/` directory thay vì arbitrary path
- Fix `vc_single()` tuple return → `_infer_file()` wrapper
- Upload model files (.pth 55MB + .index 49MB) lên HF Space repo
- Xác định root cause: **PyAV >= 10** break `channels` attribute trong `audio2()`

### Chưa giải quyết
- Dockerfile hiện có `"av<10"` → **BUILD_ERROR** trên HF
- Fix đúng: COPY `audio_patch.py` đè lên `rvc_python/lib/audio.py` trong container

## Files touched
- `opus-lucida/space/app.py`
- `opus-lucida/space/Dockerfile` ← có bug BUILD_ERROR
- `opus-lucida/automation/video/rvc_agent.py`

## Lesson learned
- HF Space git repo: KHÔNG force push — xóa model LFS files
- Chỉ dùng `huggingface_hub.HfApi.upload_file()` cho code changes
- Monkey-patch `rvc_python.lib.audio` không đủ — phải patch `rvc_python.modules.vc.modules.load_audio` (local ref từ `from ... import`)
- PyAV >= 10 thay đổi API: `ostream.channels` read-only → cần downgrade hoặc patch `audio2()`
