# Copy to .env and fill in. Without ANTHROPIC_API_KEY the report runs in template-only mode.
ANTHROPIC_API_KEY=

# Placeholder credentials for "assumed" external connections (see connections.yaml).
# Not used in MVP file mode — present only to demonstrate secret handling.
JIRA_TOKEN=
SLACK_TOKEN=
GITHUB_TOKEN=
