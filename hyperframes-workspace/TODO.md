# TODO

## Next engineering steps

- Expand `renderer/scene-json-to-hyperframes.mjs` into a reusable package
- Add full JSON Schema validation before preview and render
- Add a manifest for fonts, music, sfx, and icons
- Merge `render-project.mjs` and `render-samples.mjs` into one stable CLI with reporting

## Next template steps

- Flesh out `subtitle/`
- Flesh out `terminal/`
- Flesh out `architecture/`
- Flesh out `dashboard/`
- Flesh out `browser/`
- Flesh out `timeline/`

## Next product steps

- Add prompt packs in `prompts/` for each scene type
- Add schema examples for multi-scene videos
- Add a block catalog mapped from launch-video patterns
- Add local regression snapshots for sample renders

## Cloud steps

- Add Lambda packaging notes after local workflow is stable
- Add Cloud Run notes after local workflow is stable
- Define render artifact upload and cache strategy
