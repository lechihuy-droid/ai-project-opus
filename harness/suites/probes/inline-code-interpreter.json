{
  "id": "probe-inline-code-interpreter",
  "name": "Probe Inline Code Interpreter",
  "description": "Intentional policy violation used by security-regression: interpreter -c bypass found in the P0.3 audit.",
  "checks": [
    {
      "id": "inline-code-interpreter",
      "type": "command",
      "command": ["{py311}", "-c", "import shutil"],
      "cwd": "{root}",
      "expected_exit": 0
    }
  ]
}
