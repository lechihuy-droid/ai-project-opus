import { useState } from 'react'
import { Button, EmptyState, Input, Select } from '../lib/ui'
export default function HooksPage() { const [notice, setNotice] = useState(''); return <div className="flex h-full min-h-0 flex-col gap-space-4 p-space-6"><div className="flex items-end justify-between"><div><h1 className="text-title font-semibold text-primary">Hooks</h1><p className="mt-space-1 text-caption text-secondary">Tự động hoá sự kiện.</p></div><Button variant="primary" onClick={() => setNotice('Hooks chưa nối backend — TODO(backend)')}>New hook</Button></div>{notice && <p className="text-caption text-muted">{notice}</p>}<div className="flex gap-space-2"><Input disabled placeholder="Tìm hook…" aria-label="Tìm hook" /><Select disabled><option>Event</option></Select><Select disabled><option>Status</option></Select></div><section className="min-h-0 flex-1 overflow-auto rounded-lg border border-border-subtle bg-surface"><table className="w-full text-left"><thead><tr className="border-b border-border-subtle text-caption text-muted">{['Name', 'Event', 'Trigger point', 'Executed', 'Status', 'Last updated'].map(item => <th key={item} className="p-space-3 font-medium">{item}</th>)}</tr></thead></table><div className="p-space-6"><EmptyState title="Chưa có hook" description="Hooks chưa nối backend — TODO(backend). Trang này mới có giao diện, chưa có nguồn dữ liệu." /></div></section></div> }
