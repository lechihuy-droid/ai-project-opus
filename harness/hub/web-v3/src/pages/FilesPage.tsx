import { useState } from 'react'
import { Button, EmptyState, Input } from '../lib/ui'
export default function FilesPage() { const [notice, setNotice] = useState(''); return <div className="flex h-full min-h-0 flex-col gap-space-4 p-space-6"><div className="flex items-end justify-between"><div><h1 className="text-title font-semibold text-primary">Files</h1><p className="mt-space-1 text-caption text-secondary">Quản lý file.</p></div><Button variant="primary" onClick={() => setNotice('Quản lý file chưa nối backend — TODO(backend).')}>Upload</Button></div>{notice && <p className="text-caption text-muted">{notice}</p>}<Input disabled placeholder="Tìm file…" aria-label="Tìm file" /><section className="min-h-0 flex-1 overflow-auto rounded-lg border border-border-subtle bg-surface"><table className="w-full text-left"><thead><tr className="border-b border-border-subtle text-caption text-muted">{['Name', 'Type', 'Size', 'Updated'].map(item => <th key={item} className="p-space-3 font-medium">{item}</th>)}</tr></thead></table><div className="p-space-6"><EmptyState title="Chưa có file" description="Quản lý file chưa nối backend — TODO(backend)." /></div></section></div> }
