import { useEffect, useState, type ReactNode } from 'react'
import { ApiError, api } from '../lib/api'

type Provider = { id: string; available: boolean; detail: string }
type Agent = { id: string; provider: string; model?: string; system_prompt: string; skills: string[]; permission: 'read_only' | 'workspace_write'; risk_tier: string; budget: { seconds: number; max_calls: number } }
const colors: Record<string, string> = { claude: 'bg-claude', codex: 'bg-codex', nvidia: 'bg-nvidia', gemini: 'bg-gemini' }
const blank: Agent = { id: '', provider: 'nvidia', model: '', system_prompt: '', skills: [], permission: 'read_only', risk_tier: 'low', budget: { seconds: 60, max_calls: 10 } }

export default function AgentsPage() {
  const [agents, setAgents] = useState<Agent[]>([]); const [providers, setProviders] = useState<Provider[]>([]); const [skills, setSkills] = useState<{ name: string }[]>([]); const [agent, setAgent] = useState<Agent>(blank); const [editingId, setEditingId] = useState<string | null>(null); const [tab, setTab] = useState('Overview'); const [error, setError] = useState(''); const [saved, setSaved] = useState('')
  const load = () => { void Promise.all([api<Agent[]>('/api/agents'), api<Provider[]>('/api/providers'), api<{ name: string }[]>('/api/skill-library')]).then(([a, p, s]) => { setAgents(a); setProviders(p); setSkills(s) }).catch(e => setError(e instanceof ApiError ? e.message : 'Không thể tải agents')) }
  useEffect(load, [])
  const update = (change: Partial<Agent>) => setAgent(current => ({ ...current, ...change })); const newAgent = () => { setAgent({ ...blank, budget: { ...blank.budget } }); setEditingId(null); setTab('Overview'); setError(''); setSaved('') }
  const save = async () => { setError(''); setSaved(''); if (agents.some(row => row.id === agent.id) && editingId !== agent.id && !window.confirm(`Đã có agent "${agent.id}" — ghi đè?`)) return; try { const result = await api<Agent>('/api/agents', { method: 'POST', body: JSON.stringify(agent) }); setAgents(rows => [...rows.filter(row => row.id !== result.id), result].sort((a, b) => a.id.localeCompare(b.id))); setAgent(result); setEditingId(result.id); setSaved('Đã lưu agent.') } catch (e) { setError(e instanceof ApiError ? e.message : 'Không thể lưu agent') } }
  const remove = async () => { if (!agent.id || !window.confirm(`Xoá agent ${agent.id}?`)) return; try { await api(`/api/agents/${encodeURIComponent(agent.id)}`, { method: 'DELETE' }); setAgents(rows => rows.filter(row => row.id !== agent.id)); newAgent() } catch (e) { setError(e instanceof ApiError ? e.message : 'Không thể xoá agent') } }
  const riskTiers = [...new Set(['read_only', ...agents.map(row => row.risk_tier), agent.risk_tier])]
  return <div className="flex h-full min-h-0 flex-col gap-4 p-7"><div><div className="mb-2 text-[10px] font-semibold uppercase tracking-[.14em] text-faint">ĐIỀU PHỐI</div><h1 className="text-xl font-semibold">Agents</h1></div>{error && <div className="text-xs text-err">{error}</div>}<div className="flex min-h-0 flex-1 flex-col gap-3 overflow-hidden lg:flex-row"><aside className="w-full shrink-0 overflow-auto rounded-lg border border-line bg-panel p-2 lg:w-64"><button onClick={newAgent} className="mb-3 w-full rounded bg-codex px-3 py-2 text-xs font-semibold text-ink">+ Agent mới</button>{agents.map(row => <button key={row.id} onClick={() => { setAgent(row); setEditingId(row.id); setTab('Overview'); setSaved('') }} className={`mb-1 flex w-full items-center gap-2 rounded border px-3 py-2 text-left ${agent.id === row.id ? 'border-codex bg-panel2' : 'border-transparent hover:bg-panel2'}`}><i className={`h-2 w-2 rounded-full ${colors[row.provider] ?? 'bg-faint'}`} /><span className="min-w-0 flex-1 truncate font-mono text-xs">{row.id}</span><span className="rounded-full border border-line px-1.5 text-[10px] text-dim">{row.permission}</span></button>)}</aside><main className="min-h-0 min-w-0 flex-1 overflow-auto rounded-lg border border-line bg-panel p-4"><div className="mb-4 flex items-center gap-2">{['Overview', 'Skills', 'Budget'].map(name => <button key={name} onClick={() => setTab(name)} className={`border-b-2 px-3 py-2 text-xs ${tab === name ? 'border-codex text-text' : 'border-transparent text-dim'}`}>{name}</button>)}<button onClick={() => void save()} className="ml-auto rounded bg-codex px-3 py-1.5 text-xs font-semibold text-ink">Lưu</button>{agent.id && <button onClick={() => void remove()} className="rounded border border-err px-3 py-1.5 text-xs text-err">Xoá</button>}</div>{tab === 'Overview' && <div className="grid gap-3 md:grid-cols-2"><Field label="ID"><input disabled={agents.some(row => row.id === agent.id)} value={agent.id} onChange={e => update({ id: e.target.value })} className="control" /></Field><Field label="Provider"><select value={agent.provider} onChange={e => update({ provider: e.target.value })} className="control">{providers.map(p => <option key={p.id} value={p.id} disabled={!p.available}>{p.id}{p.available ? '' : ` — ${p.detail}`}</option>)}</select></Field><Field label="Model"><input value={agent.model ?? ''} onChange={e => update({ model: e.target.value })} className="control" /></Field><Field label="Risk tier"><select value={agent.risk_tier} onChange={e => update({ risk_tier: e.target.value })} className="control">{riskTiers.map(tier => <option key={tier}>{tier}</option>)}</select></Field><Field label="Permission"><select value={agent.permission} onChange={e => update({ permission: e.target.value as Agent['permission'] })} className="control"><option value="read_only">read_only</option><option value="workspace_write">workspace_write</option></select></Field><Field label="System prompt" wide><textarea rows={8} value={agent.system_prompt} onChange={e => update({ system_prompt: e.target.value })} className="control resize-y" /></Field></div>}{tab === 'Skills' && <div className="space-y-2">{skills.map(skill => <label key={skill.name} className="flex items-center gap-2 text-xs"><input type="checkbox" checked={agent.skills.includes(skill.name)} onChange={e => update({ skills: e.target.checked ? [...agent.skills, skill.name] : agent.skills.filter(name => name !== skill.name) })} />{skill.name}</label>)}</div>}{tab === 'Budget' && <div className="grid max-w-md gap-3 md:grid-cols-2"><Field label="Seconds"><input type="number" min="1" step="1" value={agent.budget.seconds} onChange={e => update({ budget: { ...agent.budget, seconds: Number(e.target.value) } })} className="control" /></Field><Field label="Max calls"><input type="number" min="1" step="1" value={agent.budget.max_calls} onChange={e => update({ budget: { ...agent.budget, max_calls: Number(e.target.value) } })} className="control" /></Field></div>}{saved && <div className="mt-4 text-xs text-ok">{saved}</div>}</main></div></div>
}
function Field({ label, children, wide = false }: { label: string; children: ReactNode; wide?: boolean }) { return <label className={`flex flex-col gap-1 text-[10px] uppercase tracking-wide text-faint ${wide ? 'md:col-span-2' : ''}`}>{label}{children}</label> }
